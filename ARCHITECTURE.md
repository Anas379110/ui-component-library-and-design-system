# 🏗️ المعمارية — مكتبة مكونات UI ونظام تصميم

## نظرة عامة على المعمارية
أكثر من 60 مكون منشور كحزمة npm ومستخدم عبر عدة مشاريع. المعمارية مبنية بشكل يضمن قابلية التوسع (Scalability) والصيانة (Maintainability) على المدى الطويل.

## المكوّنات الرئيسية
- **الواجهة الأمامية (Frontend):** تطبيق SPA يتواصل مع الخلفية عبر REST API (أو WebSocket للأجزاء اللحظية)، مع إدارة حالة مركزية للبيانات المشتركة بين الشاشات.
- **طبقة الـ API:** خدمة خلفية موحّدة تعرض نقاط نهاية (Endpoints) منظمة حسب الموارد (Resources)، مع تحقق من الصلاحيات على مستوى كل Endpoint.
- **طبقة منطق العمل (Business Logic):** معزولة عن طبقة الـ API نفسها لتسهيل الاختبار وإعادة الاستخدام.
- **طبقة البيانات:** قاعدة بيانات علائقية رئيسية، مع فهارس (Indexes) مضبوطة على الحقول الأكثر استعلاماً.
- **التكاملات الخارجية:** خدمات طرف ثالث مربوطة عبر طبقة وسيطة (Adapter) تعزل التطبيق عن تفاصيل كل مزوّد.

## تدفّق البيانات (Data Flow)
المستخدم يتفاعل مع الواجهة → الطلب يُرسل إلى الـ API → التحقق من الصلاحية → تنفيذ منطق العمل والتفاعل مع قاعدة البيانات أو خدمة خارجية → إرجاع الاستجابة وتحديث الحالة في الواجهة.

## قابلية التوسّع (Scalability)
النظام مصمم للتوسّع الأفقي (Horizontal Scaling) — يمكن تشغيل عدة نسخ من كل مكوّن خلف موازن حمولة (Load Balancer) دون تغيير في منطق العمل، وتمت مراعاة ذلك منذ مرحلة التصميم الأولى بدل تركه لمرحلة لاحقة.

## المرونة تجاه الأعطال (Resilience)
تم اعتماد مبدأ الفشل الآمن (Fail Gracefully): مهلات زمنية (Timeouts) محددة لكل استدعاء خارجي، إعادة محاولة (Retry) محدودة مع تأخير تصاعدي (Exponential Backoff)، وحدود قاطعة (Circuit Breaker) لمنع انهيار متسلسل بين الخدمات المترابطة.

---
*جزء من [توثيق المشروع الكامل](./PROJECT.md)*
